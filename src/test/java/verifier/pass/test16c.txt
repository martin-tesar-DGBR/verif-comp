{
    x := 12
    y := 10
    z := 70
    w := 50
    p := 1
    q := 0
    r := -7
    if x > 5 {
        x := 3
        x := 100
        if x > 5 {
            x := 3
            x := 100
            if x > 5 {
                x := 3
                x := 100
            }
            else {
                y := x + 3
            }
        }
        else {
            y := x + 3
            if x > 5 {
                x := 3
                x := 100
            }
            else {
                y := x + 3
            }
        }
    }
    else {
        y := x - 3
        if x > 5 {
            x := 3
            x := 100
            if x > 5 {
                x := 3
                x := 100
                if x > 5 {
                    x := 3
                    x := 100
                }
                else {
                    y := x + 3
                }
            }
            else {
                y := x + 3
            }
        }
        else {
            y := x + 3
            if x > 5 {
                x := 3
                x := 100
                if x > 5 {
                    x := 3
                    x := 100
                }
                else {
                    y := x + 3
                }
            }
            else {
                y := x + 3
            }
        }
    }

    check(x > y)
    check(x == x)
    check(!(y == x))
    check((z > p))
    check(z > q)

    check(q > r)
    check(p > r)
    check((r < x))
    check(y > p)
    check(r < z)
    
    check(x > y)
    check(x == x)
    check(!(y == x))
    check((z > p))
    check(z > q)

    check(q > r)
    check(p > r)
    check((r < x))
    check(y > p)
    check(r < z)

    check(x > y)
    check(x == x)
    check(!(y == x))
    check((z > p))
    check(z > q)

    check(q > r)
    check(p > r)
    check((r < x))
    check(y > p)
    check(r < z)

    check(x > y)
    check(x == x)
    check(!(y == x))
    check((z > p))
    check(z > q)

    check(q > r)
    check(p > r)
    check((r < x))
    check(y > p)
    check(r < z)

    check(x > y)
    check(x == x)
    check(!(y == x))
    check((z > p))
    check(z > q)

    check(q > r)
    check(p > r)
    check((r < x))
    check(y > p)
    check(r < z)
}
