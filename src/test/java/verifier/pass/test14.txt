{
    myvar := 10
    x := myvar * 2 - 5
    if x == 0 || myvar > 0 {
        myvar := myvar - 1
        check(myvar > 0)

        if myvar == 42 {
            myvar := myvar*myvar
            check(myvar < 0)
        }
        else {
            myvar := myvar - 1
            check(!(myvar < 0))
            check(myvar == myvar)
        }

        check(myvar < 0 || x > 0)

        if x == 15 {
            myvar := myvar*x - x*3
            check(myvar > 0)
        }
        else {
            myvar := myvar - 1
            check((myvar < 0))
            check(!(myvar == myvar))
        }
    }
    else {
        check(x < 0)
        x := -4
    }
    print(x)
    print(myvar)
    check(myvar < 0 || x > 0 && myvar > 30)
}